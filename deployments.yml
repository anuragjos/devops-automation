apiVersion: apps/v1
kind: Deployment
metadata:
    name: mydeployment
spec:
 replicas: 2
 selector:
    matchLabels:
     app: myspringbootapp
  template:
    metadata:
      lables:
        app: myspringbootapp
    spec:
    - containers:
      name: myspringbootapp
      image: anuragjoshi01/kubernetes:latest
      imagePullPolicy: Always
      ports: 
      - containerPort: 
